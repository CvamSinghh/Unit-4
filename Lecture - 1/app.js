// we need to do something on server , so we need JS to interact with databases.
//console.log(app.js);

const { first, second, fourth, third,hiddenvar} = require("./third.js")
const naam = require("./sample.json")
 //  const th = require("./third.js")


console.log(hiddenvar)
first();
second()
 console.log(third.city, fourth.hobby)



